<div class= "form-group <%= "has-error has-feedback" if resource.errors.full_messages_for(:current_password).any? %>">
  <%= f.label :current_password, class: "control-label" %>
  <%= f.password_field :current_password, autocomplete: "off", class: "form-control",
    placeholder: "Re-enter Password" %>
  <% if resource.errors.full_messages_for(:current_password).any? %>
    <span class="glyphicon glyphicon-remove form-control-feedback" aria-hidden="true"></span>
    <span class="error-explanation">
      <%= resource.errors.full_messages_for(:current_password).first %>
    </span>
  <% elsif args[:label_addons].present? %>
  	<em><%= args[:label_addons] %></em>
  <% end %>
</div>